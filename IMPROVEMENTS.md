# InnoDB MVCC 可视化系统改进文档

## 改进概览

本次更新包含以下重要改进：

### 1. ✅ 版本链序号修正
**问题**: 版本链显示时，旧版本序号大，新版本序号小
**解决**: 修改版本链显示逻辑，现在越新的版本序号越大

**修改文件**: `static/js/app.js`
- 第535行：添加 `versionNumber = versions.length - index` 计算
- 版本1是最旧的，版本N是最新的

**效果**:
- 版本1: 事务1插入的数据
- 版本2: 事务2更新的数据
- 版本3: 事务3更新的数据（最新）

---

### 2. ✅ 版本链显示顺序优化
**问题**: 版本链从旧到新显示，最新数据在底部
**解决**: 反转显示顺序，最新版本显示在最上面

**修改文件**: `static/js/app.js`
- 第578行：使用 `[...versions].reverse()` 反转版本数组
- 最新的版本（版本号最大）现在显示在顶部

**效果**: 用户首先看到最新的数据变化，更符合使用习惯

---

### 3. ✅ 选择性刷新版本链
**问题**: 版本链每3秒自动刷新，即使数据没有变化
**解决**: 只在选中的行被修改时才刷新版本链

**修改文件**: `static/js/app.js`
- 第8-9行：添加 `selectedRowId` 和 `lastModifiedRows` 全局变量
- 第227-272行：重构 `refreshSystemState()` 函数
  - 跟踪所有被修改的行
  - 检测选中行是否被修改
  - 只在必要时刷新版本链
- 第534行：在 `showVersionChain()` 中记录选中的行ID

**效果**: 减少不必要的刷新，提升性能和用户体验

---

### 4. ✅ 已提交事务显示具体数据修改
**问题**: 已提交事务只显示操作摘要，看不到具体修改的数据
**解决**: 显示每个操作的详细数据内容

**修改文件**:
- `static/js/app.js` (第358-424行)：重构 `renderCommittedTransactions()`
  - 显示每个操作的类型、行ID、时间戳
  - 显示操作的具体数据内容（通过 `op.details`）
  - 显示开始时间和提交时间

- `mvcc_system.py`：更新操作记录逻辑
  - INSERT: 记录插入的数据 `{'data': data}`
  - UPDATE: 记录旧数据和新数据 `{'old_data': old_data, 'new_data': data}`
  - DELETE: 记录被删除的数据 `{'deleted_data': deleted_data}`
  - READ: 记录读取到的数据 `{'visible': ..., 'data': data}`

**效果**: 用户可以清楚看到每个已提交事务具体修改了哪些数据

---

### 5. ✅ 活跃事务显示具体数据修改
**问题**: 活跃事务只显示操作类型，看不到具体修改的数据
**解决**: 在操作历史中显示每个操作的详细数据内容

**修改文件**: `static/js/app.js` (第274-358行)
- 重构 `renderActiveTransactions()` 函数
- 添加 `op-header` 和 `op-data` 结构来展示数据
- 显示最近5个操作的详细信息

**效果**: 用户可以实时看到活跃事务正在修改的数据内容

---

### 6. ✅ 移除持续时间字段
**问题**: 显示持续时间字段，但用户更关心开始和结束时间
**解决**: 移除所有持续时间显示，只保留开始时间和结束时间

**修改文件**:
- `transaction.py` (第66-77行)：
  - 移除 `get_duration()` 方法
  - 从 `to_dict()` 中移除 `duration` 字段

- `static/js/app.js`：
  - 活跃事务：只显示开始时间
  - 已提交事务：显示开始时间和提交时间

**效果**: 界面更简洁，信息更直观

---

### 7. ✅ 事务回滚功能完善
**问题**: 事务回滚后，版本链和数据行状态没有正确恢复
**解决**: 实现完整的回滚逻辑

**修改文件**: `mvcc_system.py` (第29-81行)
- 新增 `_rollback_row_changes()` 方法
  - **INSERT 回滚**: 删除该行，并从版本链中移除最新版本
  - **UPDATE 回滚**: 恢复旧数据，更新 `trx_id` 和 `roll_pointer`，并从版本链中移除最新版本
  - **DELETE 回滚**: 取消删除标记，恢复数据，更新指针

- 更新 `rollback_transaction()` 方法
  - 遍历事务修改的所有行
  - 对每一行调用 `_rollback_row_changes()` 进行回滚

**效果**:
- 回滚后数据行状态正确恢复
- 版本链正确回退到回滚前的状态
- 其他事务能看到正确的数据版本

---

### 8. ✅ ReadView 可见性逻辑修复
**问题**: ReadView 无法正确读取已提交事务的数据版本
**场景**:
- 事务1、2已提交
- 事务3、4活跃
- 事务4的 ReadView: `m_ids=[3,4]`, `min_trx_id=3`
- 事务4应该能看到事务1、2的数据，但实际看不到

**解决**: 修复版本链回溯逻辑

**修改文件**: `data_row.py` (第56-110行)
- 重构 `get_visible_version()` 方法
- 正确处理 Undo 日志的回溯
  - INSERT: 返回 `new_value`（插入的数据）
  - UPDATE: 返回该事务修改后的数据（前一个 Undo 的 `old_value` 或当前的 `new_value`）
  - DELETE: 返回 None
- 添加 `prev_undo_log` 变量来跟踪前一个 Undo 日志

**关键理解**:
- 当前行数据是最新版本
- Undo 日志记录的是历史版本
- 回溯时，需要找到第一个可见的事务，并返回该事务修改后的数据

**效果**:
- 事务4可以正确读取事务1、2已提交的数据
- 版本链回溯逻辑完全正确
- 所有可见性规则都符合 InnoDB MVCC 标准

---

### 9. ✅ CSS 样式优化
**问题**: 操作数据显示需要新的样式支持
**解决**: 添加 CSS 样式来美化操作数据显示

**修改文件**: `static/css/style.css` (第338-365行)
- 更新 `.operation-item` 样式，支持垂直布局
- 添加 `.op-header` 样式，显示操作头部信息
- 添加 `.op-data` 样式，显示操作数据内容
  - 使用 monospace 字体
  - 浅灰色背景
  - 边框和圆角
  - 支持换行显示

**效果**: 操作数据显示更清晰、更美观

---

## 测试验证

所有改进都经过完整测试，测试脚本：`test_mvcc.py`

### 测试场景

1. **基本可见性规则**: 验证 ReadView 的基本可见性判断
2. **UPDATE 回滚**: 验证 UPDATE 操作的回滚逻辑
3. **INSERT 回滚**: 验证 INSERT 操作的回滚逻辑
4. **DELETE 操作**: 验证 DELETE 操作的可见性
5. **DELETE 回滚**: 验证 DELETE 操作的回滚逻辑
6. **复杂多事务场景**: 验证多个事务并发修改的场景
7. **ReadView 可见性规则**: 验证 ReadView 在复杂场景下的正确性

### 运行测试

```bash
python3 test_mvcc.py
```

### 测试结果

```
总计: 7 个测试
通过: 7 个
失败: 0 个

🎉 所有测试通过！
```

---

## 使用说明

### 启动系统

```bash
# 安装依赖
pip install flask

# 启动服务器
python app.py
```

### 访问界面

打开浏览器访问: `http://127.0.0.1:5001`

### 功能演示

1. **创建事务**: 选择隔离级别，点击"开启事务"
2. **数据操作**:
   - 插入数据: 输入事务ID和JSON数据
   - 更新数据: 输入事务ID、行ID和新数据
   - 删除数据: 输入事务ID和行ID
   - 读取数据: 输入事务ID和行ID
3. **查看版本链**: 点击数据行，查看该行的完整版本链
4. **提交/回滚**: 在活跃事务卡片中点击"提交"或"回滚"按钮

### 版本链说明

- **最新版本在顶部**: 最新的数据变化显示在最上面
- **版本序号**: 越新的版本序号越大
- **自动刷新**: 只在选中行被修改时才刷新
- **详细信息**: 显示每个版本的事务ID、时间戳、数据内容、Undo日志信息

---

## 技术细节

### ReadView 可见性规则

```python
def is_visible(self, trx_id: int) -> bool:
    # 规则1: 自己修改的数据可见
    if trx_id == creator_trx_id:
        return True

    # 规则2: ReadView创建前已提交的数据可见
    if trx_id < min_trx_id:
        return True

    # 规则3: ReadView创建后才开始的事务不可见
    if trx_id > max_trx_id:
        return False

    # 规则4: 在范围内但不在活跃列表中的事务可见（已提交）
    return trx_id not in m_ids
```

### 版本链回溯逻辑

```python
def get_visible_version(self, read_view, undo_logs):
    # 1. 检查当前版本是否可见
    if read_view.is_visible(self.row.trx_id):
        return self.row.data

    # 2. 沿着Undo链回溯
    current_undo_id = self.row.roll_pointer
    prev_undo_log = None

    while current_undo_id is not None:
        undo_log = undo_logs[current_undo_id]

        # 3. 找到第一个可见的事务
        if read_view.is_visible(undo_log.trx_id):
            # 返回该事务修改后的数据
            if undo_log.log_type == INSERT:
                return undo_log.new_value
            elif undo_log.log_type == UPDATE:
                return prev_undo_log.old_value or undo_log.new_value
            elif undo_log.log_type == DELETE:
                return None

        prev_undo_log = undo_log
        current_undo_id = undo_log.roll_pointer

    return None
```

### 回滚逻辑

```python
def _rollback_row_changes(self, trx_id, row_id):
    row = self.data_row_manager.get_row(row_id)

    if row.trx_id != trx_id:
        return  # 不是该事务修改的，无需回滚

    undo_log = self.undo_log_manager.get_undo_log(row.roll_pointer)

    if undo_log.log_type == INSERT:
        # 删除该行
        self.data_row_manager.rows.pop(row_id)
        # 从版本链中移除
        version_chain.versions.pop()

    elif undo_log.log_type == UPDATE:
        # 恢复旧值
        row.data = undo_log.old_value
        row.trx_id = undo_log.trx_id if undo_log.roll_pointer else None
        row.roll_pointer = undo_log.roll_pointer
        # 从版本链中移除
        version_chain.versions.pop()

    elif undo_log.log_type == DELETE:
        # 取消删除标记
        row.deleted = False
        row.data = undo_log.old_value
        row.trx_id = undo_log.trx_id if undo_log.roll_pointer else None
        row.roll_pointer = undo_log.roll_pointer
```

---

## 总结

本次更新完善了 InnoDB MVCC 可视化系统的核心功能：

1. ✅ **版本链显示优化**: 序号正确、顺序合理、选择性刷新
2. ✅ **数据修改可视化**: 活跃和已提交事务都显示具体数据内容
3. ✅ **时间显示优化**: 移除持续时间，只显示开始和结束时间
4. ✅ **回滚功能完善**: 正确恢复数据行和版本链状态
5. ✅ **可见性逻辑修复**: ReadView 正确读取已提交事务的数据

所有功能都经过完整测试，确保系统稳定可靠。
